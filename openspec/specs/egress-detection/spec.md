# egress-detection

## Purpose

Детекция фактического egress машины: определяет, уходит ли foreign-трафик через VPN-цепочку (маяк видит `warp=on`), путём трёхслойного наблюдения — постоянная растяжка, события сетевого пути, периодические пробы — плюс РУ-маяк для автоопределения прямого IP и denylist запрещённого egress.

## Requirements

### Requirement: Классификация результата пробы маяка

Система SHALL классифицировать каждую пробу foreign-маяка (`https://www.cloudflare.com/cdn-cgi/trace`, резервный `https://1.1.1.1/cdn-cgi/trace`; ответ — plain text `key=value` со значимыми полями `ip=`, `warp=`) по правилам, где denylist `FORBIDDEN_EGRESS` сильнее всех прочих признаков:

1. HTTP 200 с `ip=`, ip ∈ `FORBIDDEN_EGRESS` (при любом `warp=`) → leak-кандидат с диагнозом по источнику совпадения;
2. HTTP 200 с `ip=`, `warp ∈ {on, plus}`, ip ∉ `FORBIDDEN_EGRESS` → protected;
3. HTTP 200 с `ip=`, `warp=off`, ip ∈ `EXPECTED_IPS`, ip ∉ `FORBIDDEN_EGRESS` → protected;
4. HTTP 200 с `ip=`, `warp=off`, ip чужой → leak-кандидат;
5. Таймаут, сетевой сбой, TLS-ошибка или ответ без `ip=` (captive portal) → offline.

#### Scenario: Защищённый egress через WARP

- **WHEN** проба вернула HTTP 200 с `warp=on` и ip, отсутствующим в `FORBIDDEN_EGRESS`
- **THEN** результат пробы — protected

#### Scenario: Чужой egress без WARP

- **WHEN** проба вернула HTTP 200 с `warp=off` и ip, отсутствующим и в `EXPECTED_IPS`, и в `FORBIDDEN_EGRESS`
- **THEN** результат пробы — leak-кандидат

#### Scenario: Egress из EXPECTED_IPS без WARP

- **WHEN** проба вернула HTTP 200 с `warp=off` и ip ∈ `EXPECTED_IPS`
- **THEN** результат пробы — protected

#### Scenario: Запрещённый egress перевешивает warp=on

- **WHEN** проба вернула HTTP 200 с `warp=on`, но ip ∈ статической части `FORBIDDEN_EGRESS` (сервер инфраструктуры)
- **THEN** результат пробы — leak-кандидат с диагнозом «цепочка вышла напрямую с сервера <ip>»

#### Scenario: Egress совпал с прямым РУ-IP

- **WHEN** проба вернула HTTP 200 с ip, равным текущему динамическому прямому РУ-IP
- **THEN** результат пробы — leak-кандидат с диагнозом «полный обход VPN: foreign-трафик идёт с прямого IP сети»

#### Scenario: Captive portal или недоступный маяк

- **WHEN** проба завершилась таймаутом, сетевой/TLS-ошибкой или вернула ответ без поля `ip=`
- **THEN** результат пробы — offline (НЕ утечка)

### Requirement: Резервный маяк

Система SHALL при недоступности основного маяка выполнять пробу через резервный (`https://1.1.1.1/cdn-cgi/trace`, обращение по IP без DNS) прежде чем классифицировать результат как offline.

#### Scenario: Основной маяк недоступен, резервный отвечает

- **WHEN** основной маяк не ответил за таймаут, а резервный вернул валидный ответ
- **THEN** классификация выполняется по ответу резервного маяка

### Requirement: Растяжка — постоянное соединение с heartbeat

Система SHALL поддерживать постоянное TLS-соединение к маяку («растяжка»), раз в `heartbeatSeconds` (15 с) отправлять по нему минимальный запрос `GET /cdn-cgi/trace` с `Connection: keep-alive` и дедлайном чтения 5 с. Любой обрыв соединения (EOF/RST/ошибка) или истечение дедлайна SHALL немедленно запускать свежую пробу. Переустановка растяжки после обрыва SHALL выполняться с экспоненциальным backoff (2 с → 4 → 8 → … максимум 60 с).

#### Scenario: Обрыв растяжки

- **WHEN** TLS-соединение растяжки получает EOF, RST или ошибку
- **THEN** немедленно запускается свежая проба маяка и начинается переустановка растяжки с backoff

#### Scenario: Heartbeat не дождался ответа

- **WHEN** ответ на heartbeat-запрос не получен за 5 с
- **THEN** соединение считается мёртвым, запускается немедленная свежая проба

#### Scenario: Backoff в офлайне

- **WHEN** переустановка растяжки повторно завершается ошибкой
- **THEN** интервалы попыток растут экспоненциально от 2 с до максимума 60 с

### Requirement: Реакция на события сетевого пути

Система SHALL наблюдать изменения сетевого пути (интерфейсы, шлюз, появление/исчезновение utun) и различать три вида событий:

1. **Пропажа сети** (путь стал unsatisfied): SHALL немедленно, без debounce и без ожидания пробы, порождать Offline-сигнал детектора (в строгом режиме — с закрывающим reconcile до каких-либо проб);
2. **Изменение пути при живой сети**: SHALL запускать немедленную пробу с debounce ~300 мс от шквала событий; из устойчивого Protected превентивное закрытие SHALL NOT выполняться (мягкая проверка — закрывает вердикт, не событие); если группы уже закрыты (Offline/Checking строгого режима), они SHALL оставаться закрыты до вердикта Protected;
3. **Повторное событие без фактической смены** (сведения о пути — интерфейсы, шлюз, доступность — идентичны предыдущему обработанному событию): SHALL игнорироваться целиком — без записи в журнал, без сброса динамического прямого РУ-IP, без пробы и без запроса РУ-маяка. Память дедупликации SHALL сбрасываться при полном пробуждении машины и при старте мониторинга: первое событие пути после этих границ обрабатывается всегда, даже если сведения о пути совпадают с досыпными.

#### Scenario: Смена сети

- **WHEN** сетевой путь изменился при живой сети (например, отключение VPN-интерфейса utun)
- **THEN** после debounce ~300 мс запускается немедленная свежая проба

#### Scenario: Шквал событий пути

- **WHEN** за 300 мс приходит несколько событий изменения пути
- **THEN** выполняется одна проба, а не по одной на каждое событие

#### Scenario: Пропажа сети распознаётся немедленно

- **WHEN** Wi-Fi выключен и путь стал unsatisfied
- **THEN** детектор переходит в Offline сразу, не дожидаясь таймаута пробы (~6 с)

#### Scenario: Частые проверки не дёргают группы из открытого состояния

- **WHEN** режим строгий, состояние Protected и события пути/обрывы растяжки приходят часто при живом VPN
- **THEN** группы не переключаются вовсе: каждая проверка подтверждает Protected без промежуточного закрытия

#### Scenario: Повторное событие с идентичным путём игнорируется

- **WHEN** приходит событие пути, чьи сведения (интерфейсы, шлюз, доступность) идентичны предыдущему обработанному событию
- **THEN** событие игнорируется: в журнал ничего не пишется, проба не запускается, прямой РУ-IP не сбрасывается, запрос РУ-маяка не выполняется

#### Scenario: Возврат сети после пропажи не дедуплицируется

- **WHEN** путь стал unsatisfied, затем вернулся с теми же интерфейсами и шлюзом
- **THEN** оба события обрабатываются: сведения различаются признаком доступности

#### Scenario: Первое событие после пробуждения обрабатывается всегда

- **WHEN** машина полностью проснулась и пришло событие пути со сведениями, идентичными досыпным
- **THEN** событие обрабатывается как смена пути: за время сна сеть могла фактически смениться на неотличимую по сведениям

### Requirement: Реакция на события системного питания

Система SHALL наблюдать события системного управления питанием как четвёртый слой детектора, не зависящий от сети, и различать два вида событий:

1. **Предстоящее засыпание**: система SHALL получать уведомление до фактического ухода машины в сон и порождать соответствующий сигнал детектора (в строгом режиме — с закрывающим reconcile, выполняемым до того, как системе будет разрешено уснуть);
2. **Полное пробуждение**: система SHALL запускать немедленную свежую пробу, не дожидаясь событий сетевого пути и не полагаясь на них.

Границы окна сна определяются только этими двумя сигналами. Dark wake пробуждением NOT считается: сигнал полного пробуждения в dark wake не приходит, а **возврат из dark wake в сон системой не сообщается вовсе** (живая приёмка 2026-08-02: maintenance sleep после dark wake — без сигнала засыпания). Поэтому всё от засыпания до полного пробуждения — одно окно сна, сколько бы dark wake'ов в него ни попало; отдельного сигнала на каждый цикл ждать нельзя.

Существование этого слоя обязательно, потому что засыпание машины НЕ порождает пропажу сети: на Apple Silicon при включённых `tcpkeepalive` и `powernap` сетевой стек во сне остаётся живым (система входит в сон с признаком `TCPKeepAlive=active`), путь не становится unsatisfied, и ни один из трёх сетевых слоёв детектора события не видит. Замер на живой машине 2026-08-02: сон 708 с из состояния Protected не породил ни одного события детектора.

#### Scenario: Засыпание не порождает пропажу сети

- **WHEN** машина уходит в сон при включённом системном keepalive сетевого стека
- **THEN** событие «путь стал unsatisfied» не возникает, и единственным сигналом о засыпании остаётся слой событий питания

#### Scenario: Сигнал засыпания приходит до сна

- **WHEN** система сообщает о предстоящем засыпании
- **THEN** детектор обрабатывает событие и (в строгом режиме) завершает закрывающий reconcile прежде, чем система уснёт

#### Scenario: Пробуждение запускает пробу без события пути

- **WHEN** машина полностью проснулась и сетевой путь при этом не изменился (та же сеть, что и до сна)
- **THEN** немедленная свежая проба всё равно выполняется — по событию пробуждения

#### Scenario: Dark wake не размыкает окно сна

- **WHEN** машина спит, происходит dark wake с сетевой активностью, затем машина возвращается в сон без сигнала засыпания
- **THEN** детектор продолжает считать машину спящей: в строгом режиме группы остаются включены весь dark wake, вердикт protected их не выключает

#### Scenario: Наблюдение не трогает группы при засыпании

- **WHEN** включён режим наблюдения (`observeOnly`), режим защиты строгий и система сообщает о предстоящем засыпании
- **THEN** группы не изменяются, а в журнал пишется, что строгий режим закрыл бы их

### Requirement: Засыпание не удерживается отказом инфраструктуры

Система SHALL всегда разрешать машине уснуть и SHALL NOT удерживать сон дольше собственного ограниченного бюджета на закрытие групп. Подтверждение системному управлению питанием SHALL отправляться по всем веткам выполнения, включая ошибочные и истёкшие по таймауту: неотправленное подтверждение обходится пользователю задержкой засыпания на весь системный таймаут.

Неудача закрытия при засыпании SHALL записываться предупреждением в журнал и SHALL взводить флаг «helper недоступен», чтобы первый же вердикт после пробуждения выполнил повторный reconcile. Эскалация (выключение Wi-Fi) при засыпании SHALL NOT выполняться: состояние Checking эскалации не подлежит, а машина всё равно уходит в сон.

#### Scenario: Helper не отвечает при засыпании

- **WHEN** режим строгий, система сообщает о предстоящем засыпании, а helper не отвечает
- **THEN** система всё равно разрешает сон по истечении бюджета, пишет предупреждение в журнал и взводит флаг «helper недоступен»; Wi-Fi не выключается

#### Scenario: Повторный reconcile после неудачного закрытия

- **WHEN** закрытие при засыпании не удалось, машина проснулась и проба вернула вердикт
- **THEN** reconcile выполняется заново по текущему состоянию, даже если состояние детектора не изменилось

#### Scenario: Штатное закрытие укладывается в бюджет

- **WHEN** режим строгий, helper и CLI работают штатно и система сообщает о предстоящем засыпании
- **THEN** группы включаются и подтверждение отправляется без исчерпания бюджета

### Requirement: Вердикты устаревших проб отбрасываются

Система SHALL применять вердикт пробы, только если с момента её запуска не произошло более нового события, обесценивающего результат: запуск более свежей пробы, изменение сетевого пути, пропажа сети или засыпание системы. Устаревший вердикт SHALL отбрасываться без изменения состояния и без reconcile.

#### Scenario: Поздний protected после пропажи сети

- **WHEN** проба запущена при живой сети, затем путь стал unsatisfied, затем проба вернула protected
- **THEN** вердикт отбрасывается; состояние остаётся Offline, группы не открываются

#### Scenario: Поздний protected после смены сети

- **WHEN** режим строгий, проба запущена в сети A, затем путь сменился на сеть B, затем проба вернула protected
- **THEN** вердикт отбрасывается: он описывает сеть A; состояние решает только вердикт свежей пробы, запущенной после debounce в сети B

#### Scenario: Поздний protected после засыпания

- **WHEN** режим строгий, проба запущена при живой сети, затем система сообщила о предстоящем засыпании (группы закрыты, состояние Checking), затем проба вернула protected
- **THEN** вердикт отбрасывается: группы НЕ открываются, машина уходит в сон закрытой

#### Scenario: Более свежая проба авторитетнее

- **WHEN** во время выполнения пробы запущена более свежая проба
- **THEN** вердикт первой пробы отбрасывается, применяется только вердикт свежей

### Requirement: Плановая свежая проба

Система SHALL раз в `probeSeconds` (60 с) выполнять свежую пробу отдельным новым соединением без кэша и переиспользования соединений, с таймаутом `probeTimeoutSeconds` (6 с). Это ловит случай «старый поток жив, новые соединения идут мимо VPN», который растяжка не видит.

#### Scenario: Тихая утечка при живой растяжке

- **WHEN** маршруты перевернулись так, что старые соединения живут по старому пути, а новые идут напрямую
- **THEN** не позднее чем через ~60 с плановая проба новым соединением обнаруживает leak-кандидата

### Requirement: Двойное подтверждение утечки

Система SHALL переводить детектор в состояние Leak только после двух leak-проб подряд с интервалом 2–3 с. Protected подтверждения NOT REQUIRED — применяется с первой пробы.

#### Scenario: Подтверждённая утечка

- **WHEN** проба дала leak-кандидата и повторная проба через 2–3 с снова дала leak
- **THEN** фиксируется переход в Leak

#### Scenario: Разовый ложняк

- **WHEN** проба дала leak-кандидата, а повторная проба через 2–3 с дала protected
- **THEN** переход в Leak не происходит

### Requirement: Мониторинг активен с запуска

Система SHALL начинать мониторинг сразу при запуске приложения (немедленная проба на старте) и SHALL выполнять немедленную пробу при появлении любой сети.

#### Scenario: Старт приложения

- **WHEN** приложение запустилось и мониторинг включён
- **THEN** немедленно выполняется проба и по её результату устанавливается состояние

#### Scenario: Появилась сеть

- **WHEN** сеть стала доступна после отсутствия
- **THEN** немедленно выполняется проба

### Requirement: Статус VPN-клиента не используется как детектор

Система SHALL NOT использовать статус VPN-клиента (например, `scutil --nc status`) как критерий защищённости. Допустимо показывать его в UI как справочную информацию.

#### Scenario: VPN-клиент сообщает «Подключено» при реальной утечке

- **WHEN** VPN-клиент считает соединение активным, но маяк видит чужой egress
- **THEN** система классифицирует состояние по маяку (leak-кандидат), игнорируя статус клиента

### Requirement: РУ-маяк — автоопределение прямого IP

Система SHALL периодически запрашивать «прямой IP» текущей сети у РУ-маяка (конфигурируемые URL; домен обязан быть в зоне `.ru`): основной `https://yandex.ru/internet/api/v0/ip` (JSON-строка `"<ip>"`), резервный `https://2ip.ru` (text/plain, требуется curl-подобный User-Agent). Ответ SHALL валидироваться как IP-адрес (v4/v6), иначе отбрасывается. Запрос выполняется при старте, при каждой фактической смене сети (повторные события пути без смены сведений дедуплицируются и запрос не порождают) и далее раз в `ruBeaconRefreshSeconds` (300 с). Полученный прямой IP образует динамическую часть `FORBIDDEN_EGRESS` и живёт до следующей фактической смены сети.

#### Scenario: Прямой IP получен

- **WHEN** РУ-маяк вернул валидный IP-адрес
- **THEN** адрес становится динамической частью `FORBIDDEN_EGRESS` и немедленно запускается свежая проба foreign-маяка

#### Scenario: Невалидный ответ РУ-маяка

- **WHEN** тело ответа РУ-маяка после разбора не является IP-адресом
- **THEN** результат отбрасывается, динамическая запись не обновляется

#### Scenario: Основной РУ-маяк недоступен

- **WHEN** основной РУ-маяк не ответил за таймаут 6 с
- **THEN** выполняется запрос к резервному РУ-маяку

#### Scenario: Повторное событие пути не трогает прямой IP

- **WHEN** прямой РУ-IP известен и приходит событие пути со сведениями, идентичными предыдущим
- **THEN** прямой IP остаётся в denylist без сброса и повторного запроса, повторной записи в журнал нет

### Requirement: Guard от РУ-маяка, прошедшего через VPN

Система SHALL NOT добавлять ответ РУ-маяка в denylist, если он совпал с текущим protected-egress (`warp=on`): это означает, что `.ru` не в сплите и запрос ушёл через VPN. В журнал SHALL записываться предупреждение.

#### Scenario: РУ-маяк вернул protected-egress

- **WHEN** ответ РУ-маяка равен текущему egress-IP при состоянии Protected
- **THEN** запись в denylist не добавляется, в журнал пишется предупреждение «РУ-маяк прошёл через VPN, сплит не работает?»

### Requirement: Сбой РУ-маяка некритичен

Сбой РУ-маяка SHALL NOT менять состояние детектора: динамическая запись denylist просто устаревает, в UI отображается её давность.

#### Scenario: РУ-маяк полностью недоступен

- **WHEN** оба РУ-маяка не отвечают
- **THEN** состояние детектора не меняется, давность прямого IP в UI растёт

### Requirement: Статическая часть FORBIDDEN_EGRESS

Система SHALL поддерживать редактируемый пользователем статический список запрещённых egress-IP (серверы инфраструктуры). Egress с любого адреса списка SHALL классифицироваться как leak-кандидат независимо от прочих признаков. Список SHALL быть пустым по умолчанию: выходные узлы у каждого пользователя свои, а приложение раздаётся другим людям (осознанное отклонение от §13 SPEC.md, где список предзаполнен адресами автора).

#### Scenario: Утечка через сервер инфраструктуры

- **WHEN** egress-IP пробы совпал с адресом из статического списка
- **THEN** результат — leak-кандидат с диагнозом «цепочка вышла напрямую с сервера <ip>» (отвал WARP на выходном узле)

#### Scenario: Первый запуск у нового пользователя

- **WHEN** приложение запускается впервые
- **THEN** статический список запрещённых egress пуст, и классификация опирается только на `warp`/`EXPECTED_IPS` и динамический прямой РУ-IP
