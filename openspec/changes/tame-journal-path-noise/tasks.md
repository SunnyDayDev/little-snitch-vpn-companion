# tasks — tame-journal-path-noise

## 1. Дедупликация событий пути в координаторе

- [x] 1.1 В `MonitoringCoordinator` завести память последнего обработанного `NetworkPathInfo`; в начале `handlePathChange` guard на равенство — при совпадении выйти до любых эффектов (журнал, сброс `directRuIP`, растяжка, generation, dispatch, debounce)
- [x] 1.2 Обновлять память только при фактической обработке события; сбрасывать её в `handleSystemDidWake` и при старте мониторинга (`start()`), чтобы первое событие после сна/старта всегда проходило полный каскад
- [x] 1.3 Тесты `MonitoringCoordinatorTests`: повторное событие с идентичным `NetworkPathInfo` не пишет в журнал, не запускает пробу, не сбрасывает `directRuIP` и не дёргает РУ-маяк
- [x] 1.4 Тесты границ дедупликации: unsatisfied → satisfied с теми же интерфейсами обрабатывается (различие в `isSatisfied`); после `handleSystemDidWake` идентичное досыпному событие обрабатывается; после паузы и возобновления мониторинга — тоже

## 2. Журнал: фильтр до лимита показа

- [x] 2.1 `AppModel.refreshJournal`: загружать записи без отсечки в 500 (`recent(limit: .max)`), порт `JournalStore` не менять
- [x] 2.2 `JournalWindow`: порядок «фильтр по категории → последние 2000»; константа лимита — рядом со списком фильтров
- [x] 2.3 Подпись окна: счётчик записей выбранной категории; при усечении — «показаны последние 2000 из M»; сверить с ds-токенами существующей подписи
- [x] 2.4 Тест на логику «фильтр до отсечки»: при >2000 записей категории показываются последние 2000 отфильтрованных, а записи категории за пределами последних 2000 общего списка не теряются (вынесено в `Domain/JournalDisplay.swift`, тесты в `DomainTests`)

## 3. Приёмка

- [x] 3.1 Прогнать полный тестовый набор (CI-паритет: `xcodebuild test`)
- [x] 3.2 Живая приёмка на машине: убедиться, что при неподвижной сети журнал не растёт капёжом «путь изменился / прямой IP / растяжка» (было ~2 600 записей/сутки), а реальная смена сети по-прежнему даёт полный каскад с записью в журнал (замер 2026-08-03: 12 минут — 0 новых записей; каскад виден в стартовом залпе 10:45:15)
- [ ] 3.3 Проверить окно журнала на дофиксовом файле (~10k записей): фильтр «Ошибки» показывает ошибки за всю неделю, подпись честно отражает усечение
