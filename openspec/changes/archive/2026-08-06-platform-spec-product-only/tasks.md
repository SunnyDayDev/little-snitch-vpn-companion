## 1. Спека

- [x] 1.1 `openspec/specs/platform-support/spec.md`, секция `## Purpose`: убрать «и автоматическая проверка обеих границ диапазона перед мержем» — правится напрямую в основной спеке, дельта `Purpose` не переносит (design, решение 5). Формулировка после правки описывает только продукт: минимальный порог запуска и запрет на API новее порога
- [x] 1.2 `openspec validate platform-spec-product-only --type change --strict` — зелёный
- [x] 1.3 Сверить дельту с основной спекой: у требования «Минимальная поддерживаемая версия macOS» в MODIFIED-блоке присутствуют все три сценария исходника («Запуск на Sequoia», «Порог зашит в оба бинаря», «Работа на текущей macOS не теряется») — MODIFIED заменяет блок целиком, пропущенный сценарий потеряется при архивации

## 2. Документация

- [x] 2.1 `README.md`, «Что понадобится» → «Для сборки»: требуется macOS 26.2+ с Xcode 26.x. Указать причину одной строкой — иконка собирается из Icon Composer-документа `App/AppIcon.icon`, а `actool` компилирует его только на macOS 26. Убрать утверждение про Xcode 26.0–26.3 на Sequoia: оно неверно после PR #14
- [x] 2.2 `README.md`, строка «Проверено» под тем же разделом: развести сборку и тесты — сборка проверяется на macOS 26.x, на образе macOS 15.7 в CI прогоняются только тесты. Живая эксплуатация на 26.x (Apple Silicon) и 15.7.8 (Intel) остаётся как есть
- [x] 2.3 Убедиться, что порог **запуска** в README нигде не поднялся: диапазон 15.7–26.x в блоке «Для запуска» и в тексте выше не тронут — это ровно то, что защищает требование «Порог запуска не завышается требованиями к сборке»
- [x] 2.4 `grep -rn "Sequoia\|15\.6\|15\.7" README.md CONTRIBUTING.md SPEC.md` — проверить, что ни один документ больше не обещает сборку на macOS ниже 26. Упоминание 15.7.8 в `SPEC.md` §1 описывает машину пользователя и правки не требует

## 3. Проверки

- [x] 3.1 `grep -n "CI\|Xcode\|мерж\|pull request" openspec/changes/platform-spec-product-only/specs/platform-support/spec.md` — совпадения допустимы только внутри блоков `**Reason**` и `**Migration**` удалённых требований; в тексте живых требований и сценариев их быть не должно
- [x] 3.2 `grep -rln "CI\|xcodebuild\|Xcode\|мерж" openspec/specs/` — после правки 1.1 список пуст: ни одна живая спека не описывает процесс создания продукта
- [x] 3.3 Продуктовые гарантии на месте: в дельте и основной спеке сохранены запуск на 15.7+ и 26.x, `minos 15.7` в обоих бинарях, `LSMinimumSystemVersion 15.7`, слайсы `arm64` + `x86_64` с нативной работой на Intel 15.7.8, запрет на API новее порога без `@available`-гейта, политика «заявленная поддержка означает проверенную»
- [x] 3.4 `ci.yml`, `project.yml`, `Scripts/` и код не изменены — `git diff --name-only` содержит только `openspec/` и `README.md`
- [x] 3.5 Джоба «Тесты (macOS 15)» продолжает быть обязательной для мержа: проверить, что ruleset защиты `main` не тронут и обе проверки требуются — удаление требования из спеки не должно ослабить фактическую проверку
