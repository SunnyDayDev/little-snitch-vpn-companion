## MODIFIED Requirements

### Requirement: Таблица переходов

State machine SHALL реализовывать переходы, часть которых зависит от режима защиты (Р — реактивный, С — строгий):

| Из | Событие | В | Действия (Р) | Действия (С) |
|---|---|---|---|---|
| любое (кроме Paused) | проба ×2 = чужой egress | Leak | reconcile: включить группы; уведомление | то же |
| Leak | проба = защищён | Protected | reconcile: выключить группы; уведомление | то же |
| Offline/Checking | проба = защищён | Protected | reconcile по результату | reconcile: выключить группы |
| Protected | обрыв растяжки / смена пути (сеть жива) | — | немедленная проба | немедленная проба; группы НЕ трогать (мягкая проверка: закрывает вердикт, не событие) |
| Offline/Checking | обрыв растяжки / смена пути (сеть жива) | — (Р) / Checking (С) | немедленная проба | Checking; группы остаются закрыты; немедленная проба |
| любое (кроме Paused) | сеть пропала (path unsatisfied) | Offline | группы НЕ трогать | закрыть группы |
| Protected/Leak | пробы не отвечают | Offline | группы НЕ трогать | закрыть группы |
| любое (кроме Paused) | система засыпает | — (Р) / Checking (С) | группы НЕ трогать; проб не запускать | закрыть группы, ЗАТЕМ разрешить системе уснуть |
| Paused | система засыпает | Paused | группы НЕ трогать | закрывающий reconcile (идемпотентно); состояние Paused сохраняется |
| любое (кроме Paused) | система проснулась (ПОЛНОЕ пробуждение) | — | немедленная проба | немедленная проба; группы остаются закрыты до вердикта Protected |
| Checking (во сне) | проба = защищён (dark wake) | Checking | — (реактивный сон состояний не создаёт) | группы НЕ открывать: вердикт во сне не открывает |
| любое | пользователь: Пауза | Paused | группы НЕ трогать; детектор остановить | закрыть группы, ЗАТЕМ остановить детектор |
| Paused | пользователь: Возобновить | по пробе (Р) / Checking (С) | немедленная проба + reconcile | закрыто; открытие только по вердикту Protected |
| Leak | helper не отвечает | Leak | эскалация, если включена | то же |
| старт приложения | — | по пробе (Р) / Checking (С) | немедленная проба + reconcile | СНАЧАЛА закрыть группы, потом проба |

Мягкие проверки из устойчивого Protected — осознанное решение: события пути и обрывы растяжки часты, вердикт может протухнуть сразу после проверки и без них, а каждое превентивное закрытие — потерянные запросы. Закрытие в строгом режиме наступает по фактам (offline-вердикт, пропажа сети, подтверждённая утечка) и на границах неопределённости (старт, возврат сети, возобновление, выход, засыпание), а не по каждому поводу для проверки.

Засыпание — граница неопределённости, а не «повод для проверки»: вердикт Protected протухает вместе с сессией, а проверить его во сне некому. Сон не порождает пропажу сети (см. `egress-detection`), поэтому строка «сеть пропала» этот случай не покрывает — нужна отдельная. Из любого не-Paused состояния засыпание в строгом режиме ведёт в Checking единообразно: диагноз прошлой сессии после сна недостоверен, включая диагноз Leak.

Сон — липкое условие от засыпания до ПОЛНОГО пробуждения, и dark wake — тоже сон: уход из dark wake обратно в сон системой не сообщается (живая приёмка 2026-08-02: maintenance sleep без сигнала засыпания, машина проспала 204 с с группами, открытыми по честному Protected-вердикту dark wake). Поэтому в строгом режиме вердикт Protected, полученный во сне, SHALL NOT открывать группы — состояние остаётся Checking; закрывающие вердикты (Leak, Offline) действуют как обычно. Реактивного режима липкость не касается: его контракт «блок только при доказанной утечке» действует и во сне.

#### Scenario: Подтверждённая утечка из Protected

- **WHEN** состояние Protected и две пробы подряд дали чужой egress
- **THEN** состояние становится Leak, выполняется reconcile (включить группы маппинга) и отправляется уведомление

#### Scenario: Восстановление из Leak

- **WHEN** состояние Leak и проба дала protected
- **THEN** состояние становится Protected, выполняется reconcile (выключить группы) и отправляется уведомление

#### Scenario: Пробы перестали отвечать (реактивный)

- **WHEN** режим реактивный, состояние Protected или Leak и пробы не отвечают (offline-результаты)
- **THEN** состояние становится Offline, группы LS не изменяются

#### Scenario: Пробы перестали отвечать (строгий)

- **WHEN** режим строгий, состояние Protected и пробы не отвечают
- **THEN** состояние становится Offline и выполняется закрывающий reconcile (группы включаются)

#### Scenario: Смена сети из открытого состояния — мягкая проверка

- **WHEN** режим строгий, состояние Protected и сетевой путь изменился (сеть жива)
- **THEN** состояние остаётся Protected и группы не трогаются; запускается немедленная проба, и закрывает только её вердикт (offline → закрыть; утечка ×2 → Leak)

#### Scenario: Сеть вернулась в строгом режиме

- **WHEN** режим строгий, состояние Offline (группы включены) и сеть появилась
- **THEN** состояние становится Checking, группы остаются включены, запускается немедленная проба; группы выключаются только после вердикта protected

#### Scenario: Сеть вернулась в реактивном режиме

- **WHEN** режим реактивный, состояние Offline и проба ответила
- **THEN** состояние становится Protected или Leak по результату пробы и выполняется reconcile

#### Scenario: Засыпание из Protected (строгий)

- **WHEN** режим строгий, состояние Protected и система сообщает о предстоящем засыпании
- **THEN** состояние становится Checking и выполняется закрывающий reconcile (группы включаются) ДО того, как системе будет разрешено уснуть

#### Scenario: Засыпание из Leak (строгий)

- **WHEN** режим строгий, состояние Leak (группы уже включены) и система сообщает о предстоящем засыпании
- **THEN** состояние становится Checking, закрывающий reconcile выполняется идемпотентно, группы остаются включены

#### Scenario: Засыпание в реактивном режиме

- **WHEN** режим реактивный, состояние Protected и система сообщает о предстоящем засыпании
- **THEN** состояние не меняется, группы LS не трогаются, проб не запускается

#### Scenario: Засыпание на паузе

- **WHEN** состояние Paused и система сообщает о предстоящем засыпании
- **THEN** состояние остаётся Paused; в строгом режиме выполняется закрывающий reconcile (идемпотентно, страховка от неудавшегося закрытия при постановке на паузу), в реактивном группы не трогаются

#### Scenario: Пробуждение в строгом режиме

- **WHEN** режим строгий, состояние Checking (группы включены после засыпания) и система полностью проснулась
- **THEN** запускается немедленная свежая проба, группы остаются включены; выключаются только по вердикту protected

#### Scenario: Вердикт Protected во сне не открывает

- **WHEN** режим строгий, машина уснула (группы включены), в dark wake проба вернула protected
- **THEN** группы остаются включены, состояние остаётся Checking; открытие возможно только по вердикту после полного пробуждения

#### Scenario: Утечка во сне закрывает как обычно

- **WHEN** режим строгий, машина спит и две пробы dark wake подряд дали чужой egress
- **THEN** фиксируется Leak с reconcile (группы включены — идемпотентно) и уведомлением; закрывающие вердикты во сне не подавляются

#### Scenario: Пробуждение в реактивном режиме

- **WHEN** режим реактивный и система проснулась
- **THEN** запускается немедленная свежая проба, состояние устанавливается по её вердикту

#### Scenario: Пробуждение на паузе

- **WHEN** состояние Paused и система проснулась
- **THEN** проб не запускается, переходов нет

#### Scenario: Пауза пользователем (реактивный)

- **WHEN** режим реактивный и пользователь ставит мониторинг на паузу из любого состояния
- **THEN** состояние становится Paused, детектор останавливается, группы LS не изменяются

#### Scenario: Пауза пользователем (строгий)

- **WHEN** режим строгий и пользователь ставит мониторинг на паузу из состояния Protected
- **THEN** сначала выполняется закрывающий reconcile (группы включаются), затем состояние становится Paused и детектор останавливается

#### Scenario: Возобновление после паузы (строгий)

- **WHEN** режим строгий и пользователь возобновляет мониторинг из Paused
- **THEN** состояние становится Checking (группы остаются включены), выполняется немедленная проба; группы выключаются только по вердикту protected

#### Scenario: В паузе утечка игнорируется

- **WHEN** состояние Paused и egress фактически чужой
- **THEN** никакие пробы не выполняются, переходов нет

#### Scenario: Старт приложения (строгий)

- **WHEN** режим строгий и мониторинг стартует
- **THEN** состояние становится Checking, закрывающий reconcile выполняется ДО первой пробы, затем запускается проба

#### Scenario: Вердикт устаревшей пробы отбрасывается после пропажи сети

- **WHEN** проба запущена, затем сеть пропала (path unsatisfied), затем проба вернула protected
- **THEN** вердикт отбрасывается: состояние остаётся Offline, открывающий reconcile не выполняется

### Requirement: Инвариант строгого режима

В строгом режиме защиты система SHALL гарантировать: управляемые группы выключены (foreign-трафик открыт) тогда и только тогда, когда последний вердикт детектора — Protected и мониторинг активен. Во всех остальных ситуациях (Leak, Offline, Checking, Paused, мониторинг выключен, приложение не работает, **машина спит**) группы SHALL быть включены.

Сон — точка потери контроля наравне с завершением приложения и выключением машины: приложение не может наблюдать egress, пока система спит, а вердикт Protected, полученный до засыпания, после пробуждения недостоверен. Поэтому инвариант SHALL обеспечиваться **до** засыпания, а не восстанавливаться после пробуждения: любое пробуждение — как полное, так и служебное (dark wake) — SHALL начинаться с включённых групп, и группы SHALL оставаться включены на всём окне сна, включая dark wake: возврат из dark wake в сон системой не сообщается, поэтому «открыто в dark wake» означало бы «открыто на весь следующий отрезок сна».

#### Scenario: Единственная дверь в «открыто»

- **WHEN** режим строгий и группы фактически выключены
- **THEN** последним применённым вердиктом был Protected при активном мониторинге

#### Scenario: Выключение тумблера мониторинга закрывает

- **WHEN** режим строгий, состояние Protected и пользователь выключает тумблер «Мониторинг»
- **THEN** перед остановкой детектора выполняется закрывающий reconcile (группы включаются)

#### Scenario: Машина спит с включёнными группами

- **WHEN** режим строгий, машина уснула из состояния Protected и спит
- **THEN** управляемые группы включены всё время сна — независимо от того, гаснет ли при засыпании сетевой интерфейс

#### Scenario: Служебное пробуждение проходит закрытым целиком

- **WHEN** режим строгий, машина спит и происходит служебное пробуждение (dark wake) с живой сетью и работающим VPN
- **THEN** группы включены весь dark wake: даже вердикт Protected не выключает их, потому что возврат в сон не будет сообщён и открытое осталось бы открытым на весь следующий отрезок сна

#### Scenario: Открытие — только после полного пробуждения

- **WHEN** режим строгий, машина полностью проснулась после сна с dark wake'ами
- **THEN** запускается свежая проба, и её вердикт Protected выключает группы — первое открытие после засыпания происходит не раньше полного пробуждения
